{
  "name": "status-monitor-web",
  "version": "1.0.0",
  "private": true,
  "description": "Web gateway for the Report Monitor dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check test/integration.test.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "mqtt": "^5.14.1"
  },
  "devDependencies": {
    "aedes": "^1.1.1"
  }
}
